{
  "name": "colorfulfiles",
  "version": "1.3.0",
  "main": "main.js",
  "description": "A compact, draggable 16-color gloss palette manager to color-code your files and folders in the sidebar.",
  "author": "Abdul Moid",
  "license": "MIT",
  "scripts": {
    "dev": "tsup src/main.ts --format cjs --minify --watch --external obsidian --out-dir .",
    "build": "tsup src/main.ts --format cjs --minify --external obsidian --out-dir ."
  },
  "devDependencies": {
    "obsidian": "^1.5.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
